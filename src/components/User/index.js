import UserList from "./UserList";
import UserCreate from "./UserCreate";
import UserEdit from "./UserEdit";
const user = {
  list: UserList,
  create: UserCreate,
  edit: UserEdit,
};

export default user;
